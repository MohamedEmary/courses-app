NODE_ENV=
# Local MongoDB root credentials (the official MONGO_INITDB_ROOT_* names)
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
# App connection URI
MONGODB_URI=
# Database URI for integration tests (must end in _test)
TEST_MONGODB_URI=
# node -e "console.log(require('crypto').randomBytes(#number).toString('hex'))"
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# Application port
PORT=